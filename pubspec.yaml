name: callpanion_elderly
description: CallPanion Elderly App with native call experience
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_callkit_incoming: ^3.0.0
  firebase_messaging: ^16.0.1
  firebase_core: ^4.1.0
  uuid: ^4.0.0
  shared_preferences: ^2.2.2
  http: ^1.1.0
  json_annotation: ^4.8.1
  cupertino_icons: ^1.0.2

  permission_handler: ^12.0.1
  connectivity_plus: ^6.0.5
  wakelock_plus: ^1.2.8
  
  # ElevenLabs dependencies
  dio: ^5.3.2
  envied: ^1.3.0
  
  # WebRTC dependencies - ElevenLabs SDK sudah include WebRTC
  # flutter_webrtc: ^0.9.48 # Tidak diperlukan, ElevenLabs SDK sudah include
  
  # Audio dependencies
  just_audio: ^0.9.36
  audio_session: ^0.1.16

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^6.7.1
  build_runner: ^2.4.7
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/sounds/
